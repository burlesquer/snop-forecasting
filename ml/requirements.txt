# ── Data ─────────────────────────────────────────
kaggle>=2.0.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0          # parquet I/O

# ── Forecasting ──────────────────────────────────
# nixtla family — unified API for Naive/MA/LightGBM/quantile
mlforecast>=0.13.0
utilsforecast>=0.2.0
datasetsforecast>=0.0.27
lightgbm>=4.3.0
statsforecast>=1.7.0     # Naive, MA baselines

# ── Explainability ───────────────────────────────
shap>=0.45.0

# ── Schema & validation ──────────────────────────
pydantic>=2.7.0
datamodel-code-generator>=0.25.0  # pydantic → TypeScript

# ── Testing ──────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0

# ── Utils ────────────────────────────────────────
python-dotenv>=1.0.0
rich>=13.7.0             # pretty CLI logs
